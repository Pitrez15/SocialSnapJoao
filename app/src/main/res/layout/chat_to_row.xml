<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp">

    <TextView
        android:id="@+id/textViewChatTo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@android:color/darker_gray"
        android:maxWidth="240dp"
        android:padding="16dp"
        android:text="This is my message"
        app:layout_constraintBottom_toBottomOf="@+id/imageViewChatTo"
        app:layout_constraintEnd_toStartOf="@+id/imageViewChatTo"
        app:layout_constraintTop_toTopOf="@+id/imageViewChatTo" />

    <ImageView
        android:id="@+id/imageViewChatTo"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_baseline_account_box_24" />
</androidx.constraintlayout.widget.ConstraintLayout>